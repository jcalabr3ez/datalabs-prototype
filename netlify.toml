[build]
  publish = "."
  command = "python3 scripts/inject_data.py"
  functions = "netlify/functions"

# Public release: pages are indexable. Social/share cards and canonical
# URLs live in each HTML head. Attach a Pioneer hostname in the Netlify
# domain settings when that DNS is ready; until then the live origin is
# https://datalabsai.netlify.app.
